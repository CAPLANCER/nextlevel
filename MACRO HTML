Public Function GetRowCount(sheetName As String) As Long
    Dim ws As Worksheet
    On Error GoTo SheetNotFound
    Set ws = ThisWorkbook.Worksheets(sheetName)
    GetRowCount = ws.UsedRange.Rows.Count
    Exit Function
SheetNotFound:
    GetRowCount = -1 'error code for missing sheet
End Function
